{
  "name": "japantok-bot",
  "version": "1.0.0",
  "type": "module",
  "description": "AI-powered chatbot for Japan Tok Mongolia auto parts",
  "scripts": {
    "dev": "vercel dev",
    "build:css": "tailwindcss -i ./src/input.css -o ./public/styles.css --minify",
    "check": "node --check api/chat.js && node --check api/sheet.js && node --check api/validate-products.js && node --check public/app.js && node --check public/widget.js && echo '✅ All files validated'",
    "check:products": "node scripts/check-products.js"
  },
  "dependencies": {
    "node-fetch": "^3.0.0"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.17",
    "postcss": "^8.4.35",
    "tailwindcss": "^3.4.10"
  }
}
